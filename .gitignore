*.jar
.DS_Store
*.pyc
digest.txt
build/program
program*

build/*.o
build/*.s
build/*.map
build/bbc/

# Large build artifacts not worth committing.
build/*.uef
build/*.wav
build/*.pt
# Exception: the deployable BBC-target checkpoint is checked in so the C build
# is reproducible without re-training (n=56, fine-tuned with boilerplate strip
# + gendered-name dedup; valid loss ~1.0; ~162 KB).
!build/bitnet_quant_n56_v200_dedup_stripped_v2_finetune.pt
build/*.sim6502
build/*.apple2
build/*.bbc
build/*_train.log
build/test_harness.*

# Regeneratable filtered/tokenized dataset caches (rebuilt by dataset/data.py).
dataset/data/cache/

# Source TinyStories data — too large for git (~2 GB train).
dataset/data/TinyStories-*.txt

# Claude / agent scratch.
.claude/
CLAUDE.md
*_DEBUG_REPORT.md
*_TOKENIZER_REPORT.md